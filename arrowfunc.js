let add = (a,b) => {
return a + b;
}
console.log(add(5,5))

// const add = (x, y) => x + y;  // without return & {}
// const add = (x, y) => {return x + y}; // with return & {}

// const divide = x => 20 / x; //single paramter without () 
// let res = divide(2);

